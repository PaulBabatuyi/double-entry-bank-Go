A simple FinTech (bank) backend built with Golang in a standard and secure way (double-entry), for now it will only handle core features like user transfer, deposit and withdrawal. to avoid over engineering and waste of time and resource, will learn how is be done and for easy future feature addition/versioning (forward/backward  compatibility, database horizontal scaling, code maintainability)  