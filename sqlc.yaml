version: "2"
sql:
  - engine: "postgresql"
    queries: "postgres/queries/"
    schema: "postgres/migrations/"
    gen:
      go:
        package: "sqlc"
        out: "postgres/sqlc"
        emit_json_tags: true
        emit_interface: true
        emit_exact_table_names: false
